# Core agent
langgraph>=0.2.50
langchain>=0.3.7
langchain-openai>=0.2.8
openai>=1.40.0

# Arize tracing
arize-otel>=0.7.0
openinference-instrumentation-langchain>=0.1.29

# Frontend + config
gradio>=4.44.0
python-dotenv>=1.0.1

# Evals (ax CLI is installed separately: `pip install arize-ax-cli`)
arize>=7.19.0
